#include <stdio.h>

struct aeroplane
{
    int FlightNumber;
    int RemainingOil;   //time in ticks
    int TimeToLand;     //time in ticks
};

int main()
{
    int currentTick = 0;
    while(1 == 1)
    {
        
        currentTick++;
    }

    return 0;
}