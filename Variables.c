#include <stdio.h>
#include <stdlib.h>

// main function

int main()
{
    // char Name[] = "Adam";
    // int Age = 67;
    // printf("There once was a man named %s.\n", Name);
    // printf("He was %d years old.\n", Age);
    
    // Age = 35;
    // printf("He realy liked the name %s.\n", Name);
    // printf("He did not like being %d years old.\n", Age);

    // int a = 10, b = 20, c;

    // if (a < b) {
    //     c = a;
    // }
    // else {
    //     c = b;
    // }

    students[]

    if (null != students && students.Length > 0)            if (students.Length > 0 && null != students)
    {
        student[0]
    }


    if (null != s)          (s != null)
    {
        s.Left()
    }

    // int d = a < b ? a : b;
    //printf("%d", d);

    // switch(d)
    // {
    //     case 10:
    //         printf('d has a value');
    //         break;
    //     case 20:
    //         printf('d has b value');
    //         break;
    //     default:
    //         ;
    //         break;
    // }


    // for(int i = 0; i < 100; i+= 5)
    // {
    //     printf("%d\n", i);
    // }

    // int j = 10;
    //return (j++);         //return j
    //++j;

    //return 0;
    char c = 'A';
    int i = c | 'B';

    printf("%d", i);


}