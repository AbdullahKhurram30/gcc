#include <stdio.h>
#include <stdlib.h>

int main()
{

    //no parenthesis or inverted commas needed for integer
    int Age = 40;
    //two types pf decimal representations: floats and doubles.
    double GPA = 3.7;
    //character storing, store only one character, use single quotation marks
    char Grade = 'A';
    //the only data types that are needed which can be used to represnt.
    //string is not exactly a data type but it is an array of characters. 
    char Phrase[] = "Faisalabad";
    //you can modify the top 3 but can not modify a string

    return 0;

}